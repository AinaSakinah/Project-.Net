@model IEnumerable<MvcTDI.Models.Item>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.inventory_item)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.inventory_desc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.supplier_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.worker_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.inventory_item)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.inventory_desc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.supplier_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.worker_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.inventory_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.inventory_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.inventory_id })
        </td>
    </tr>
}

</table>
